{
  "name": "HTTP Errors on Checkout Service",
  "expression": "sum by (service_name, service_namespace) (increase({otel_metric_name=\"dash0.spans\", service_name=\"checkoutservice\", http_response_status_code=~\"[45][0-9][0-9]\"}[1m])) > $__threshold",
  "summary": "HTTP Errors on Checkout Service",
  "description": "HTTP 4xx/5xx error span count on checkoutservice is higher than the threshold per minute. Only counts spans with explicit HTTP 4xx or 5xx status codes — gRPC spans (which carry http_response_status_code=0) are excluded.",
  "modes": ["error"],
  "labels": {},
  "annotations": {},
  "interval": "1m",
  "thresholds": {
    "degraded": 75,
    "failed": 200
  }
}